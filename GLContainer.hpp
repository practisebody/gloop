#pragma once

_GL_BEGIN
// OpenGL container objects
// It can not be shared between contexts
class GLContainer : public GLStandardObject
{
public:
	//inline const GLuint GetID() const
	//{
	//	return m_id;
	//}

	//inline GLuint& ID()
	//{
	//	return m_id;
	//}

private:
	//std::map<
};
_GL_END